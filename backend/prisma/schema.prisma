
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// model User {
//   id              String       @id @default(auto()) @map("_id") @db.ObjectId
//   name            String
//   email           String       @unique
//   password        String
//   bio             String?
//   profilePic      String?
//   graduationYear  Int?
//   branch          String?
//   degree          String?
//   college         String?
//   cgpa            Float?
//   company         String?
//   position        String?
//   experienceYears Int?
//   skills          String[]
//   interests       String[]
//   socialLinks     SocialLinks?
//   connections     String[] // store User IDs as ObjectId strings
//   posts           Post[]
//   comments        Comment[] // ✅ added this line
//   createdAt       DateTime     @default(now())

//   threads  Thread[] // ✅ REQUIRED FIX
//   messages Message[] // ✅ REQUIRED FIX
// }

//created for different roles
model User {
  id              String       @id @default(auto()) @map("_id") @db.ObjectId
  name            String
  email           String       @unique
  password        String

  role            Role?        // 👈 ADDED

  // Student fields
  bio             String?
  graduationYear  Int?
  branch          String?
  degree          String?
  college         String?
  cgpa            Float?

  // Alumni fields
  company         String?
  position        String?
  experienceYears Int?

  // Common
  profilePic      String?
  // JSON profile object to store role-specific fields
  profile         Json?
  skills          String[]
  interests       String[]
  socialLinks     SocialLinks?
  connections     String[]
  posts           Post[]
  comments        Comment[]
  createdAt       DateTime     @default(now())

  threads         Thread[]
  messages        Message[]
}

enum Role {
  STUDENT
  ALUMNI
}

type SocialLinks {
  linkedin  String?
  github    String?
  portfolio String?
  twitter   String?
}

model Post {
  id        String    @id @default(auto()) @map("_id") @db.ObjectId
  authorId  String    @db.ObjectId
  author    User      @relation(fields: [authorId], references: [id])
  title     String
  content   String
  imageUrl  String?
  createdAt DateTime  @default(now())
  likes     Int       @default(0)
  comments  Comment[]
}

model Comment {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  postId    String   @db.ObjectId
  post      Post     @relation(fields: [postId], references: [id])
  authorId  String   @db.ObjectId
  author    User     @relation(fields: [authorId], references: [id])
  content   String
  createdAt DateTime @default(now())
}

model Thread {
  id          String    @id @default(auto()) @map("_id") @db.ObjectId
  title       String
  createdById String    @db.ObjectId
  createdBy   User      @relation(fields: [createdById], references: [id])
  createdAt   DateTime  @default(now())
  messages    Message[]
}

model Message {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  threadId  String   @db.ObjectId
  thread    Thread   @relation(fields: [threadId], references: [id])
  authorId  String   @db.ObjectId
  author    User     @relation(fields: [authorId], references: [id])
  content   String
  parentId  String?  @db.ObjectId
  upvotes   String[] // store User IDs as ObjectId strings
  edited    Boolean  @default(false)
  createdAt DateTime @default(now())
}
